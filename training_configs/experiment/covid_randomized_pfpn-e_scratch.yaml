# @package _global_
logger:
    project_name: null
training:
    num_epochs: null
    num_steps: 2500
sweep_prefix: 'covid_scratch_pfpn'

defaults:
    - override /model: covid_pfpn
    - override /dataset: covid_randomized

dataset:
    dataset_rebalance_kwargs:
        multiple_datasets_mode: first
