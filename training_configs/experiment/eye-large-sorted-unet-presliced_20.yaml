# @package _global_
logger:
    project_name: null
sweep_prefix: unet_presliced_20

defaults:
    - override /model: eyes_large
    - override /checkpoint: sort-large
    - override /dataset: eyes_triple_20
    
training:
    num_steps: 1000
    num_epochs: null
