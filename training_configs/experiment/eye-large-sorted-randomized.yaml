# @package _global_
logger:
    project_name: null
sweep_prefix: deeplab_sorted

defaults:
    - override /model: eyes_large_deeplab
    - override /checkpoint: sort-large
    - override /dataset: eyes_randomized

dataset:
    dataset_rebalance_kwargs:
        multiple_datasets_mode: default