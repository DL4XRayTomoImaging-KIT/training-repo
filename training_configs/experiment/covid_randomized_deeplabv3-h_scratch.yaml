# @package _global_
logger:
    project_name: null
training:
    num_epochs: null
    num_steps: 2500
sweep_prefix: 'covid_scratch_deeplab'

defaults:
    - override /model: covid_deeplabv3
    - override /dataset: covid_randomized
    - override /optimizer: adam_head

dataset:
    dataset_rebalance_kwargs:
        multiple_datasets_mode: first
