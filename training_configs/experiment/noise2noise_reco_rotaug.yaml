# @package _global_
logger:
    project_name: noise2noise

defaults:
    - override /model: noise2noise
    - override /dataset: noise2noise_reco_rotaug
    - override /criterion: l1

training:
    num_epochs: 500
