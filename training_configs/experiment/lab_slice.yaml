# @package _global_
logger:
    project_name: noise2noise_labsource

defaults:
    - override /model: noise2noise_atro
    - override /dataset/gatherers: lab_slice
    - override /dataset/datasets: n2n_atro_reco
    - override /criterion: l1
    - override /callbacks: noise2noise
    # - override /scheduler: cyclic
training:
    num_epochs: 100
