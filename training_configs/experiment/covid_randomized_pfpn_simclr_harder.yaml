# @package _global_
logger:
    project_name: null
training:
    num_epochs: 50
sweep_prefix: 'covid_simclr_harder'

defaults:
    - override /model: covid_pfpn_simclr_harder
    - override /dataset: covid_randomized