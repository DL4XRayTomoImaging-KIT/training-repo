# @package _global_
logger:
    project_name: null
training:
    num_epochs: null
    num_steps: 2500
sweep_prefix: 'covid_simclr_pfpn'

defaults:
    - override /model: covid_pfpn
    - override /dataset: covid_randomized

model:
    backbone_checkpoint: '/home/ws/tb0536/logs/covid/2021-11-18_15-55-43/logdir/checkpoints/best.pth'

dataset:
    dataset_rebalance_kwargs:
        multiple_datasets_mode: first
