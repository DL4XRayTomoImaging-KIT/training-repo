# @package _global_
logger:
    project_name: 'medaka-interpolation'

defaults:
    - override /model: eyes_large
    - override /checkpoint: sort-large
    - override /dataset: eyes_triple
    
training:
    verbose: False
    num_epochs: 100
