# @package _global_
logger:
    project_name: covid-sorting
runner:
  runner_name: SupervisedRunner
callbacks: 
    - {'name': 'mean_displacement_callback'}

defaults:
    - override /model: covid_sorting-50
    - override /dataset: covid_sorting_32
    - override /criterion: sorting-1

training:
    num_epochs: 32
