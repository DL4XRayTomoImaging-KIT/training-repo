# @package _global_
logger:
    project_name: null
training:
    num_epochs: 50
sweep_prefix: 'covid_scratch'

defaults:
    - override /model: covid_pfpn_scratch
    - override /dataset: covid_randomized