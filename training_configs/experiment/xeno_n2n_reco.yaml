# @package _global_
logger:
    project_name: noise2noise

defaults:
    - override /model: noise2noise
    - override /dataset: xeno_n2n_reco
    - override /criterion: l1
    - override /callbacks: noise2noise

training:
    num_epochs: 700
