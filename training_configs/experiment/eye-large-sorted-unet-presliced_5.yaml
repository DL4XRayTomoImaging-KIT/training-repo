# @package _global_
logger:
    project_name: null
sweep_prefix: unet_presliced_5

defaults:
    - override /model: eyes_large
    - override /checkpoint: sort-large
    - override /dataset: eyes_triple_5
    
training:
    num_steps: 1000
    num_epochs: null
