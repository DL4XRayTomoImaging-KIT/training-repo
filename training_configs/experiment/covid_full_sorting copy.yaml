# @package _global_
logger:
    project_name: covid-segmenting
training:
    num_epochs: 250
optimizer:
    parameters_to_exclude: ['encoder.']

defaults:
    - override /model: covid
    - override /checkpoint: sorting
    - override /dataset: covid