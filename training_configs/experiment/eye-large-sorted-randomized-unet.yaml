# @package _global_
logger:
    project_name: null
sweep_prefix: unet_sorted

defaults:
    - override /model: eyes_large
    - override /checkpoint: sort-large
    - override /dataset: eyes_randomized

dataset:
    dataset_rebalance_kwargs:
        multiple_datasets_mode: default

training:
    num_epochs: null
    num_steps: 2500
