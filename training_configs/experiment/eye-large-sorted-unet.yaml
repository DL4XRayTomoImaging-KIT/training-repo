# @package _global_
logger:
    project_name: 'medaka-interpolation'

defaults:
    - override /model: eyes_large
    - override /checkpoint: sort-large
    - override /dataset: eyes
    
dataset:
    dataset_rebalance_kwargs:
        multiple_datasets_mode: default
training:
    verbose: False
    num_epochs: 300
