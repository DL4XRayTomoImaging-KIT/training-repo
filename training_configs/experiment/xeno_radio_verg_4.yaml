# @package _global_
logger:
    project_name: noise2noise_xeno

defaults:
    - override /model: noise2noise
    - override /dataset/gatherers: xeno_radio_verg_4
    - override /dataset/datasets: n2s_05_both
    - override /criterion: l1
training:
    num_epochs: 500
