# @package _global_
logger:
    project_name: brain_tumor

defaults:
    - override /model: single_class_decathlon
    # - override /dataset/gatherers: full
    - override /dataset/datasets: brain_tumor
    - override /callbacks: single_class_decathlon

task_name: Task01_BrainTumour
num_volumes: 1.0