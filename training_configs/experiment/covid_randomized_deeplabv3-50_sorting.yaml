# @package _global_
logger:
    project_name: null
training:
    num_epochs: null
    num_steps: 2500
sweep_prefix: 'covid_sorting_deeplab-50'

defaults:
    - override /model: covid_deeplabv3-50
    - override /checkpoint: sorting-50
    - override /dataset: covid_randomized

dataset:
    dataset_rebalance_kwargs:
        multiple_datasets_mode: first
