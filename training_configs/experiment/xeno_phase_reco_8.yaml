# @package _global_
logger:
    project_name: noise2noise_xeno_phase

defaults:
    - override /model: noise2noise
    - override /dataset/gatherers: xeno_phase_reco_8
    - override /dataset/datasets: n2s_05_both_scale
    - override /criterion: l1
    - override /callbacks: noise2noise
training:
    num_epochs: 500
