# @package _global_
logger:
    project_name: null
training:
    num_epochs: null
    num_steps: 3500
sweep_prefix: 'lungs_scratch_2GPU_deeplab'

defaults:
    - override /model: lungs_deeplabv3
    - override /dataset: lungs_randomized

dataset:
    dataset_rebalance_kwargs:
        multiple_datasets_mode: first
