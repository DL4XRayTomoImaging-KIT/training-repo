# @package _global_
defaults:
    - override /model: eyes_unet
    - override /checkpoint: UNet-sorting-0.05
    - override /dataset: visual_test

dataset:
  destination:
    folder: '/mnt/LSDF/users/zharov/medakas_segmentation_for_comparison/UNet-sorting-0.05'
processing:
  batch_size: 8

