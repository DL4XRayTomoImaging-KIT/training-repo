# @package _global_
defaults:
    - override /model: eyes_unet
    - override /checkpoint: UNet-scratch-1.0
    - override /dataset: visual_test

dataset:
  destination:
    folder: '/mnt/LSDF/users/zharov/medakas_segmentation_for_comparison/UNet-scratch-1.0'
processing:
  batch_size: 8

